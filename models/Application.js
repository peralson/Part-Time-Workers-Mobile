class Application {
  constructor (
      id,
      state,
      offerId,
      userId,
  ) {
      this.id = id
      this.state = state
      this.offerId = offerId
      this.userId = userId
  }
}

export default Application